package com.caravan.caravan.repository;

import com.caravan.caravan.entity.GuidLocationEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GuidLocationRepository extends JpaRepository<GuidLocationEntity, Long> {
}